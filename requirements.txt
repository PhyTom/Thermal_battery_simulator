# Thermal Battery Simulation - Dipendenze

# Core (obbligatorie)
numpy>=1.21.0
scipy>=1.7.0
pyyaml>=6.0

# Visualizzazione 3D
pyvista>=0.38.0

# GUI
PyQt6>=6.4.0
pyvistaqt>=0.9.0

# Accelerazione CPU (opzionale, raccomandata)
numba>=0.55.0
pyamg>=5.0.0  # Multigrid preconditioner per mesh grandi

# Salvataggio stato (raccomandata)
h5py>=3.7.0  # Formato HDF5 per salvataggio/caricamento stato

# Accelerazione GPU (opzionale - scegli in base alla tua GPU)
# Per NVIDIA: pip install cupy-cuda11x  (o cuda12x)
# Per AMD/Intel: pip install pyopencl
# pyopencl>=2022.1  # Decommenta se hai GPU AMD/Intel

# Sviluppo e test
pytest>=7.0.0
pytest-cov>=4.0.0

# Utility
tqdm>=4.60.0
matplotlib>=3.5.0  # Per plot 2D e grafici transitorio
